services:
  iqraai-docs:
    container_name: abdofallah/iqraai-docs
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
      - PORT=5000